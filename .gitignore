__pycache__
*.egg-info
.jupyterlite.doit.db

# JAX persistent compilation cache
.cache/

# Per-run test diagnostics (tests/refs.py:dump).  NOT goldens and never
# compared — regenerated by every march, so committing them would churn the
# repo on each run.
tests/refs/diagnostics/
